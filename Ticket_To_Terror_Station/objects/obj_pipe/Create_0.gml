event_inherited();

isBroken = false;

Interact() {
    // if pipe is broken 
    // if player has wrench 
    // fix pipe 
    // stop water from flowing out (visual sprite) (might do a quick 3 frame animation
}