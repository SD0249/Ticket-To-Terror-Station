// Inherit the parent event
event_inherited();

Interact = function () {}

Hold = function (_pickUp){
    // move object to correct 
    // may need resize 
    // may need to change bounds of image 
        // like for ladder so only a secction of ladder is visable in the corner
    // disable item in scene 
}

Place = function() {
    /// certwain locations in map where it can be placed 
    /// right under lights on the building
}